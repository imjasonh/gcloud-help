NAME
    gcloud infra-manager automigrationconfig - manage auto migration config
        resources

SYNOPSIS
    gcloud infra-manager automigrationconfig COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    Manage auto migration config resources.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        Describe an AutoMigrationConfig.

     disable-auto-migration
        Disable AutoMigrationConfig.

     enable-auto-migration
        Enable AutoMigrationConfig.
