NAME
    gcloud backup-dr service-config - manage Backup and DR Service
        configuration

SYNOPSIS
    gcloud backup-dr service-config COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    Manage Backup and DR Service configuration.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     init
        Initialize a Backup and DR Service configuration.

NOTES
    This variant is also available:

        $ gcloud alpha backup-dr service-config

