NAME
    gcloud beta vector-search collections data-objects - manage Vector Search
        data objects

SYNOPSIS
    gcloud beta vector-search collections data-objects COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage Vector Search data objects.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     aggregate
        (BETA) Aggregate data objects.

     batch-create
        (BETA) Create a batch of data objects.

     batch-delete
        (BETA) Delete a batch of data objects.

     batch-search
        (BETA) Batch search data objects from a Vector Search collection.

     batch-update
        (BETA) Update a batch of data objects.

     create
        (BETA) Create a data object.

     delete
        (BETA) Delete a data object.

     describe
        (BETA) Describe a data object.

     query
        (BETA) Query data objects from a Vector Search collection.

     search
        (BETA) Search data objects from a Vector Search collection.

     update
        (BETA) Update a data object.

NOTES
    This command is currently in beta and might change without notice.

