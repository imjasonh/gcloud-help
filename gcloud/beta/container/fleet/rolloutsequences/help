NAME
    gcloud beta container fleet rolloutsequences - create and manage rollout
        sequences

SYNOPSIS
    gcloud beta container fleet rolloutsequences COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Create and manage rollout sequences.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Create a rollout sequence resource.

     delete
        (BETA) Delete a rollout sequence resource.

     describe
        (BETA) Describe a rollout sequence.

     list
        (BETA) List all rollout sequences.

     update
        (BETA) Update a rollout sequence resource.

NOTES
    This command is currently in beta and might change without notice. This
    variant is also available:

        $ gcloud alpha container fleet rolloutsequences

