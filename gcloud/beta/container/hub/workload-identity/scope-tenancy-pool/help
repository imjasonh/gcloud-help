NAME
    gcloud beta container hub workload-identity scope-tenancy-pool - manage
        Workload Identity Feature scope tenancy pool

SYNOPSIS
    gcloud beta container hub workload-identity scope-tenancy-pool COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage Workload Identity Feature scope tenancy pool.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     set
        (BETA) Set Scope Tenancy Pool.

     unset
        (BETA) Unset Scope Tenancy Pool.

NOTES
    This command is currently in beta and might change without notice. This
    variant is also available:

        $ gcloud alpha container hub workload-identity scope-tenancy-pool

