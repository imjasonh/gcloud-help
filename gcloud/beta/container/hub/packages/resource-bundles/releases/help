NAME
    gcloud beta container hub packages resource-bundles releases - commands for
        managing Package Rollouts Releases

SYNOPSIS
    gcloud beta container hub packages resource-bundles releases COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) See gcloud beta container fleet packages resource-bundles releases
    --help for help.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Create Package Rollouts Release.

     delete
        (BETA) Delete Package Rollouts Release.

     describe
        (BETA) Describe Package Rollouts Release.

     list
        (BETA) List Releases of a Resource Bundle.

     update
        (BETA) Update Package Rollouts Release.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud container hub packages resource-bundles releases

        $ gcloud alpha container hub packages resource-bundles releases

