NAME
    gcloud beta iam workforce-pools providers scim-tenants tokens - manage IAM
        workforce identity pool provider SCIM tenant tokens

SYNOPSIS
    gcloud beta iam workforce-pools providers scim-tenants tokens COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Commands for creating, describing, listing, updating, and deleting
    SCIM tokens associated with IAM workforce identity pool provider SCIM
    tenants.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Create an IAM workforce identity pool provider SCIM tenant
        token.

     delete
        (BETA) Delete an IAM workforce identity pool provider SCIM tenant
        token.

     describe
        (BETA) Describe an IAM workforce identity pool provider SCIM tenant
        token.

     list
        (BETA) List IAM workforce identity pool provider SCIM tenant tokens.

     undelete
        (BETA) Undelete an IAM workforce identity pool provider SCIM tenant
        token.

     update
        (BETA) Update an IAM workforce identity pool provider SCIM tenant
        token.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud iam workforce-pools providers scim-tenants tokens

        $ gcloud alpha iam workforce-pools providers scim-tenants tokens

