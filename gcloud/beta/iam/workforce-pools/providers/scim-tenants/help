NAME
    gcloud beta iam workforce-pools providers scim-tenants - manage IAM
        workforce identity pool provider SCIM tenants

SYNOPSIS
    gcloud beta iam workforce-pools providers scim-tenants GROUP | COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Commands for creating, describing, listing, updating, and deleting
    SCIM tenants associated with IAM workforce identity pool providers. SCIM
    tenants enable automated user and group provisioning.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

GROUPS
    GROUP is one of the following:

     tokens
        (BETA) Manage IAM workforce identity pool provider SCIM tenant tokens.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Create an IAM workforce identity pool provider SCIM tenant.

     delete
        (BETA) Delete an IAM workforce identity pool provider SCIM tenant.

     describe
        (BETA) Describe an IAM workforce identity pool provider SCIM tenant.

     list
        (BETA) List IAM workforce identity pool provider SCIM tenants.

     undelete
        (BETA) Undelete an IAM workforce identity pool provider SCIM tenant.

     update
        (BETA) Update an IAM workforce identity pool provider SCIM tenant.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud iam workforce-pools providers scim-tenants

        $ gcloud alpha iam workforce-pools providers scim-tenants

