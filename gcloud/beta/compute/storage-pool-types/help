NAME
    gcloud beta compute storage-pool-types - read storage pool types

SYNOPSIS
    gcloud beta compute storage-pool-types COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Read Compute Engine storage pool types.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (BETA) Describe a storage pool type.

     list
        (BETA) View storage pools types.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud compute storage-pool-types

        $ gcloud alpha compute storage-pool-types

