NAME
    gcloud beta compute reservations sub-blocks - manage Compute Engine
        reservation sub blocks

SYNOPSIS
    gcloud beta compute reservations sub-blocks COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage Compute Engine reservation sub blocks.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (BETA) Describe a Compute Engine reservation sub-block.

     list
        (BETA) List Compute Engine reservation sub-blocks.

     perform-maintenance
        (BETA) Perform maintenance on a reservation sub-block.

     report-subblock-as-faulty
        (BETA) Report a sub-block within a reservation as faulty.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud compute reservations sub-blocks

        $ gcloud alpha compute reservations sub-blocks

