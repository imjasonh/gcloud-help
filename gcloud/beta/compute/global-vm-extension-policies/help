NAME
    gcloud beta compute global-vm-extension-policies - compute Engine global VM
        extension policies

SYNOPSIS
    gcloud beta compute global-vm-extension-policies COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Compute Engine global VM extension policies.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Create a Compute Engine global VM extension policy.

     delete
        (BETA) Delete a Compute Engine global VM extension policy.

     describe
        (BETA) Describe a Compute Engine global VM extension policy.

     list
        (BETA) List Compute Engine global VM extension policies.

     update
        (BETA) Update a Compute Engine global VM extension policy.

NOTES
    This command is currently in beta and might change without notice. This
    variant is also available:

        $ gcloud alpha compute global-vm-extension-policies

