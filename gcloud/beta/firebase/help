NAME
    gcloud beta firebase - work with Google Firebase

SYNOPSIS
    gcloud beta firebase GROUP [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) To view all options available for using Firebase Test Lab, run:

        $ gcloud beta firebase test --help

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

GROUPS
    GROUP is one of the following:

     test
        (BETA) Interact with Firebase Test Lab.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud firebase

        $ gcloud alpha firebase

