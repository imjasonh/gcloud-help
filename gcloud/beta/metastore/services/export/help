NAME
    gcloud beta metastore services export - export metadata from a Dataproc
        Metastore service

SYNOPSIS
    gcloud beta metastore services export COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Export metadata from a Dataproc Metastore service.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     gcs
        (BETA) Export metadata from a Dataproc Metastore service to Google
        Cloud Storage.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud metastore services export

        $ gcloud alpha metastore services export

