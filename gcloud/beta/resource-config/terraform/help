NAME
    gcloud beta resource-config terraform - the command group for Terraform
        provider configuration

SYNOPSIS
    gcloud beta resource-config terraform COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) The command group for Terraform provider configuration.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     generate-import
        (BETA) Generate Terraform import script for exported resources.

NOTES
    This command is currently in beta and might change without notice. This
    variant is also available:

        $ gcloud alpha resource-config terraform

