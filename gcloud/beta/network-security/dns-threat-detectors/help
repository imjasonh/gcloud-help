NAME
    gcloud beta network-security dns-threat-detectors - manage Dns Threat
        Detector resources

SYNOPSIS
    gcloud beta network-security dns-threat-detectors COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage Dns Threat Detector resources.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Create a DnsThreatDetector resource.

     delete
        (BETA) Delete a DnsThreatDetector resource.

     describe
        (BETA) Describe a DnsThreatDetector resource.

     list
        (BETA) List DnsThreatDetector resources.

     update
        (BETA) Update a DnsThreatDetector resource.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud network-security dns-threat-detectors

        $ gcloud alpha network-security dns-threat-detectors

