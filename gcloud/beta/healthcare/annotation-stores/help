NAME
    gcloud beta healthcare annotation-stores - manage Cloud Healthcare API
        annotation stores

SYNOPSIS
    gcloud beta healthcare annotation-stores GROUP | COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage Cloud Healthcare API annotation stores.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

GROUPS
    GROUP is one of the following:

     evaluate
        (BETA) Manage Cloud Healthcare API Annotation store evaluates.

     export
        (BETA) Manage Cloud Healthcare API Annotation store exports.

     import
        (BETA) Manage Cloud Healthcare API Annotation store imports.

COMMANDS
    COMMAND is one of the following:

     add-iam-policy-binding
        (BETA) Add an IAM policy binding to a Cloud Healthcare API annotation
        store.

     create
        (BETA) Create a Cloud Healthcare API annotation store.

     delete
        (BETA) Delete a Cloud Healthcare API annotation store.

     describe
        (BETA) Describe a Cloud Healthcare API annotation store.

     get-iam-policy
        (BETA) Retrieve the IAM policy for a Cloud Healthcare API annotation
        store.

     list
        (BETA) List Cloud Healthcare API annotation stores.

     remove-iam-policy-binding
        (BETA) Remove an IAM policy binding from a Cloud Healthcare API
        annotation store.

     set-iam-policy
        (BETA) Set the IAM policy for a Cloud Healthcare API annotation store.

     update
        (BETA) Update a Cloud Healthcare API annotation store.

NOTES
    This command is currently in beta and might change without notice. This
    variant is also available:

        $ gcloud alpha healthcare annotation-stores

