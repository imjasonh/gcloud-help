NAME
    gcloud beta healthcare datasets - manage Cloud Healthcare API datasets

SYNOPSIS
    gcloud beta healthcare datasets COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage Cloud Healthcare API datasets.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     add-iam-policy-binding
        (BETA) Add an IAM policy binding to a Cloud Healthcare API dataset.

     create
        (BETA) Create a Cloud Healthcare API dataset.

     deidentify
        (BETA) Create a new Cloud Healthcare API dataset containing
        de-identified data from the source dataset.

     delete
        (BETA) Delete a Cloud Healthcare API dataset.

     describe
        (BETA) Describe a Cloud Healthcare API dataset.

     get-iam-policy
        (BETA) Retrieve the IAM policy for a Cloud Healthcare API dataset.

     list
        (BETA) List Cloud Healthcare API datasets.

     remove-iam-policy-binding
        (BETA) Remove an IAM policy binding to a Cloud Healthcare API dataset.

     set-iam-policy
        (BETA) Set the IAM policy for a Cloud Healthcare API dataset.

     update
        (BETA) Update a Cloud Healthcare API dataset.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud healthcare datasets

        $ gcloud alpha healthcare datasets

