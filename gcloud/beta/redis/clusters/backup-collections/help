NAME
    gcloud beta redis clusters backup-collections - manage backup collections
        of Memorystore for Redis Cluster instances

SYNOPSIS
    gcloud beta redis clusters backup-collections COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage backup collections of Memorystore for Redis Cluster
    instances.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (BETA) Show metadata for a backup collection.

     list
        (BETA) List backup collections in a region.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud redis clusters backup-collections

        $ gcloud alpha redis clusters backup-collections

