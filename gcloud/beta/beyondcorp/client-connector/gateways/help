NAME
    gcloud beta beyondcorp client-connector gateways - create and manipulate
        BeyondCorp client gateways

SYNOPSIS
    gcloud beta beyondcorp client-connector gateways COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Regional, server-side components to which clients can connect.
    Client gateways are deployed by administrators. The gateways communicate
    with the BeyondCorp Enterprise enforcement system to enforce context-aware
    checks. The BeyondCorp Enterprise enforcement system uses Identity-Aware
    Proxy and Access Context Manager, a flexible BeyondCorp Enterprise zero
    trust policy engine.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Create a new BeyondCorp client gateway.

     delete
        (BETA) Delete a single BeyondCorp client gateway.

     describe
        (BETA) Describe a single BeyondCorp client gateway.

     list
        (BETA) List BeyondCorp client gateway resources.

NOTES
    This command is currently in beta and might change without notice. This
    variant is also available:

        $ gcloud alpha beyondcorp client-connector gateways

