NAME
    gcloud beta filestore instances snapshots - create and manage Filestore
        snapshots

SYNOPSIS
    gcloud beta filestore instances snapshots COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Create and manage Filestore snapshots.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Create a Filestore snapshot.

     delete
        (BETA) Delete a Filestore snapshot.

     describe
        (BETA) Display information about a Filestore snapshot.

     list
        (BETA) List Filestore snapshots.

     update
        (BETA) Update the description or labels of a Filestore snapshot.

NOTES
    This command is currently in beta and might change without notice. This
    variant is also available:

        $ gcloud filestore instances snapshots

