NAME
    gcloud beta network-services endpoint-policies - manage Network Services
        EndpointPolicies

SYNOPSIS
    gcloud beta network-services endpoint-policies COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage Network Services EndpointPolicies.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     delete
        (BETA) Delete endpoint policy.

     describe
        (BETA) Describe an endpoint policy.

     export
        (BETA) Export endpoint policy.

     import
        (BETA) Import endpoint policy.

     list
        (BETA) List endpoint policies.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud network-services endpoint-policies

        $ gcloud alpha network-services endpoint-policies

