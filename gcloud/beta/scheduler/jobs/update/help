NAME
    gcloud beta scheduler jobs update - update Cloud Scheduler jobs for various
        types of targets

SYNOPSIS
    gcloud beta scheduler jobs update COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Update Cloud Scheduler jobs for various types of targets.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     app-engine
        (BETA) Update a Cloud Scheduler job with an App Engine target.

     http
        (BETA) Update a Cloud Scheduler job that triggers an action via HTTP.

     pubsub
        (BETA) Update a Cloud Scheduler job with a Pub/Sub target.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud scheduler jobs update

        $ gcloud alpha scheduler jobs update

