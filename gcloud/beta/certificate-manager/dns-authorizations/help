NAME
    gcloud beta certificate-manager dns-authorizations - manage Certificate
        Manager DNS authorizations

SYNOPSIS
    gcloud beta certificate-manager dns-authorizations COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Commands for managing DNS authorizations.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Create a DNS Authorization.

     delete
        (BETA) Delete a DNS Authorization.

     describe
        (BETA) Show details about a DNS Authorization.

     list
        (BETA) List all DNS Authorizations in a project.

     update
        (BETA) Update a DNS Authorization.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud certificate-manager dns-authorizations

        $ gcloud alpha certificate-manager dns-authorizations

