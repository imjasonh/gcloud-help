NAME
    gcloud beta certificate-manager maps entries - manage Certificate Manager
        certificate map entries

SYNOPSIS
    gcloud beta certificate-manager maps entries COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Commands for managing certificate map entries.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Create a certificate map entry.

     delete
        (BETA) Delete a certificate map entry.

     describe
        (BETA) Describe an existing certificate map entry.

     list
        (BETA) List certificate map entries.

     update
        (BETA) Update a certificate map entry.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud certificate-manager maps entries

        $ gcloud alpha certificate-manager maps entries

