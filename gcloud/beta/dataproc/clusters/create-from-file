NAME
    gcloud beta dataproc clusters create-from-file - create a cluster from a
        file

SYNOPSIS
    gcloud beta dataproc clusters create-from-file --file=FILE [--async]
        [--region=REGION] [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Create a cluster from a file.

EXAMPLES
    To create a cluster from a YAML file, run:

        $ gcloud beta dataproc clusters create-from-file --file=cluster.yaml

REQUIRED FLAGS
     --file=FILE
        The path to a YAML file containing a Dataproc Cluster resource.

        For more information, see:
        https://cloud.google.com/dataproc/docs/reference/rest/v1/projects.regions.clusters#Cluster.

OPTIONAL FLAGS
     --async
        Return immediately, without waiting for the operation in progress to
        complete.

     --region=REGION
        Dataproc region to use. Each Dataproc region constitutes an independent
        resource namespace constrained to deploying instances into Compute
        Engine zones inside the region. Overrides the default dataproc/region
        property value for this command invocation.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --access-token-file, --account,
    --billing-project, --configuration, --flags-file, --flatten, --format,
    --help, --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

NOTES
    This command is currently in beta and might change without notice. This
    variant is also available:

        $ gcloud alpha dataproc clusters create-from-file

