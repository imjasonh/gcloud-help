NAME
    gcloud beta builds repositories - manage repositories for Google Cloud
        Build

SYNOPSIS
    gcloud beta builds repositories COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Manage repositories for Google Cloud Build.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (BETA) Create a Cloud Build repository.

     delete
        (BETA) Delete a Cloud Build Repository.

     describe
        (BETA) Describe a Cloud Build Repository.

     list
        (BETA) List all Cloud Build repositories in a connection.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud builds repositories

        $ gcloud alpha builds repositories

