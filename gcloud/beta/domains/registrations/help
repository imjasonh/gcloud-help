NAME
    gcloud beta domains registrations - manage Cloud Domains registrations

SYNOPSIS
    gcloud beta domains registrations GROUP | COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (BETA) Commands for managing Cloud Domains registrations.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

GROUPS
    GROUP is one of the following:

     authorization-code
        (BETA) Manage Cloud Domains registration's authorization code.

     configure
        (BETA) Configure Cloud Domains registrations' management, DNS or
        contact settings.

     operations
        (BETA) Manage Cloud Domains operations.

COMMANDS
    COMMAND is one of the following:

     delete
        (BETA) Delete a Cloud Domains registration.

     describe
        (BETA) Describe an existing Cloud Domains registration.

     export
        (BETA) (DEPRECATED) Export a Cloud Domains registration.

     get-register-parameters
        (BETA) Get register parameters (including availability) of a specific
        domain.

     get-transfer-parameters
        (BETA) (DEPRECATED) Get transfer parameters of a specific domain.

     import
        (BETA) (DEPRECATED) Import a domain from Google Domains registrar to
        Cloud Domains.

     list
        (BETA) List Cloud Domains registrations.

     list-importable-domains
        (BETA) (DEPRECATED) List Google Domains registrations importable into
        Cloud Domains.

     register
        (BETA) Register a new domain.

     search-domains
        (BETA) Search for available domains.

     transfer
        (BETA) (DEPRECATED) Transfer a domain from another registrar.

     update
        (BETA) Update a Cloud Domains registration.

NOTES
    This command is currently in beta and might change without notice. These
    variants are also available:

        $ gcloud domains registrations

        $ gcloud alpha domains registrations

