NAME
    gcloud compute tpus tpu-vm service-identity - commands for Cloud TPU VM
        service identity

SYNOPSIS
    gcloud compute tpus tpu-vm service-identity COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    Commands for Cloud TPU VM service identity.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        Create a Cloud TPU VM service identity for a project.

NOTES
    This variant is also available:

        $ gcloud alpha compute tpus tpu-vm service-identity

