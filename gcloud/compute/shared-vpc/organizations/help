NAME
    gcloud compute shared-vpc organizations - configure organizations for
        Shared VPC networking

SYNOPSIS
    gcloud compute shared-vpc organizations COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    Configure organizations for Shared VPC networking.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     list-host-projects
        List shared VPC host projects in a given organization.

NOTES
    These variants are also available:

        $ gcloud alpha compute shared-vpc organizations

        $ gcloud beta compute shared-vpc organizations

