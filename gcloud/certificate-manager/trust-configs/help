NAME
    gcloud certificate-manager trust-configs - manage Certificate Manager trust
        configs

SYNOPSIS
    gcloud certificate-manager trust-configs COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    Manage Certificate Manager trust configs.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        Create TrustConfig.

     delete
        Delete TrustConfig.

     describe
        Show details about a TrustConfig.

     export
        Export TrustConfig.

     import
        Import TrustConfig.

     list
        List all TrustConfigs in a project.

NOTES
    These variants are also available:

        $ gcloud alpha certificate-manager trust-configs

        $ gcloud beta certificate-manager trust-configs

