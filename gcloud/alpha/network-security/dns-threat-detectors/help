NAME
    gcloud alpha network-security dns-threat-detectors - manage Dns Threat
        Detector resources

SYNOPSIS
    gcloud alpha network-security dns-threat-detectors COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Dns Threat Detector resources.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a DnsThreatDetector resource.

     delete
        (ALPHA) Delete a DnsThreatDetector resource.

     describe
        (ALPHA) Describes a DnsThreatDetector resource.

     list
        (ALPHA) Lists DnsThreatDetector resources.

     update
        (ALPHA) Update a DnsThreatDetector resource.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud beta network-security dns-threat-detectors

