NAME
    gcloud alpha domains registrations google-domains-dns - manage deprecated
        Google Domains DNS configuration

SYNOPSIS
    gcloud alpha domains registrations google-domains-dns COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Commands for managing DNS records and forwarding configuration of
    Cloud Domains registrations that use deprecated Google Domains DNS.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     export-dns-record-sets
        (ALPHA) Export your registration's Google Domains DNS zone's
        record-sets into a file.

     get-forwarding-config
        (ALPHA) Get forwarding configuration of a specific Cloud Domains
        registration.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud domains registrations google-domains-dns

        $ gcloud beta domains registrations google-domains-dns

