NAME
    gcloud alpha pubsub message-transforms - manage Cloud Pub/Sub message
        transforms

SYNOPSIS
    gcloud alpha pubsub message-transforms COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Cloud Pub/Sub message transforms.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     test
        (ALPHA) Tests message transforms against a given message.

     validate
        (ALPHA) Validates a message transform.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

