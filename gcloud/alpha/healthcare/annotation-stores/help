NAME
    gcloud alpha healthcare annotation-stores - manage Cloud Healthcare API
        annotation stores

SYNOPSIS
    gcloud alpha healthcare annotation-stores GROUP | COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Cloud Healthcare API annotation stores.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

GROUPS
    GROUP is one of the following:

     evaluate
        (ALPHA) Manage Cloud Healthcare API Annotation store evaluates.

     export
        (ALPHA) Manage Cloud Healthcare API Annotation store exports.

     import
        (ALPHA) Manage Cloud Healthcare API Annotation store imports.

COMMANDS
    COMMAND is one of the following:

     add-iam-policy-binding
        (ALPHA) Add an IAM policy binding to a Cloud Healthcare API annotation
        store.

     create
        (ALPHA) Create a Cloud Healthcare API annotation store.

     delete
        (ALPHA) Delete a Cloud Healthcare API annotation store.

     describe
        (ALPHA) Describe a Cloud Healthcare API annotation store.

     get-iam-policy
        (ALPHA) Retrieve the IAM policy for a Cloud Healthcare API annotation
        store.

     list
        (ALPHA) List Cloud Healthcare API annotation stores.

     remove-iam-policy-binding
        (ALPHA) Remove an IAM policy binding from a Cloud Healthcare API
        annotation store.

     set-iam-policy
        (ALPHA) Set the IAM policy for a Cloud Healthcare API annotation store.

     update
        (ALPHA) Update a Cloud Healthcare API annotation store.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud beta healthcare annotation-stores

