NAME
    gcloud alpha healthcare fhir-stores import - manage Cloud Healthcare API
        FHIR store imports

SYNOPSIS
    gcloud alpha healthcare fhir-stores import COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Cloud Healthcare API FHIR store imports.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     gcs
        (ALPHA) Import FHIR resources from Google Cloud Storage into a Cloud
        Healthcare API FHIR store.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud healthcare fhir-stores import

        $ gcloud beta healthcare fhir-stores import

