NAME
    gcloud alpha builds enterprise-config bitbucketserver - manage Bitbucket
        Server configurations for Cloud Build

SYNOPSIS
    gcloud alpha builds enterprise-config bitbucketserver COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Bitbucket Server configurations for Cloud Build.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a Bitbucket Server config for use by Cloud Build.

     delete
        (ALPHA) Delete a Bitbucket Server config from Cloud Build.

     describe
        (ALPHA) Describe a Bitbucket Server config in a Google Cloud project.

     list
        (ALPHA) List all Bitbucket Server configs in a Google Cloud project.

     update
        (ALPHA) Update a Bitbucket Server config for use by Cloud Build.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

