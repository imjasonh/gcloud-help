NAME
    gcloud alpha builds enterprise-config - manage Enterprise configurations
        for Google Cloud Build

SYNOPSIS
    gcloud alpha builds enterprise-config GROUP [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Enterprise configurations for Google Cloud Build.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

GROUPS
    GROUP is one of the following:

     bitbucketserver
        (ALPHA) Manage Bitbucket Server configurations for Google Cloud Build.

     github
        (ALPHA) Manage GitHub Enterprise configurations for Google Cloud Build.

     gitlab
        (ALPHA) Manage GitLab Enterprise configurations for Google Cloud Build.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

