NAME
    gcloud alpha network-services multicast-groups - manage Network Services
        MulticastGroups

SYNOPSIS
    gcloud alpha network-services multicast-groups COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Network Services MulticastGroups.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a multicast group.

     delete
        (ALPHA) Delete a multicast group.

     describe
        (ALPHA) Describe a multicast groups.

     list
        (ALPHA) List multicast groups.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

