NAME
    gcloud alpha network-services service-bindings - manage Network Services
        Bindings

SYNOPSIS
    gcloud alpha network-services service-bindings COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Network Services Bindings.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a service binding.

     delete
        (ALPHA) Delete service binding.

     describe
        (ALPHA) Describe a service binding.

     list
        (ALPHA) List service bindings.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud network-services service-bindings

        $ gcloud beta network-services service-bindings

