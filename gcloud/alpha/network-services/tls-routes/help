NAME
    gcloud alpha network-services tls-routes - manage Network Services
        TlsRoutes

SYNOPSIS
    gcloud alpha network-services tls-routes COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Network Services TlsRoutes.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     delete
        (ALPHA) Delete tls route.

     describe
        (ALPHA) Describe a tls route.

     export
        (ALPHA) Export tls route.

     import
        (ALPHA) Import tls route.

     list
        (ALPHA) List tls routes.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud network-services tls-routes

