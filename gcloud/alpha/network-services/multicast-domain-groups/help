NAME
    gcloud alpha network-services multicast-domain-groups - manage Network
        Services MulticastDomainGroups

SYNOPSIS
    gcloud alpha network-services multicast-domain-groups COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Network Services MulticastDomainGroups.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a multicast domain group.

     delete
        (ALPHA) Delete a multicast domain group.

     describe
        (ALPHA) Describe a multicast domain group.

     list
        (ALPHA) List multicast domain groups.

     update
        (ALPHA) Update a multicast domain group.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud network-services multicast-domain-groups

        $ gcloud beta network-services multicast-domain-groups

