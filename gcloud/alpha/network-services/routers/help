NAME
    gcloud alpha network-services routers - manage Network Services Routers

SYNOPSIS
    gcloud alpha network-services routers COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Network Services Routers.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     delete
        (ALPHA) Delete router.

     export
        (ALPHA) Export router.

     import
        (ALPHA) Import router.

     list
        (ALPHA) List routers.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

