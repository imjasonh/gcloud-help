NAME
    gcloud alpha memorystore backup-collections backups - manage Backup
        resources

SYNOPSIS
    gcloud alpha memorystore backup-collections backups COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Backup resources.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     delete
        (ALPHA) Delete backups.

     describe
        (ALPHA) Describe backups.

     export
        (ALPHA) export backups.

     list
        (ALPHA) List backups.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud memorystore backup-collections backups

        $ gcloud beta memorystore backup-collections backups

