NAME
    gcloud alpha sql ssl server-certs - provide commands for managing server
        certificates of Cloud SQL instances

SYNOPSIS
    gcloud alpha sql ssl server-certs COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Provide commands for managing server certificates of Cloud SQL
    instances, including creating, listing, rotating in, and rolling back
    certificates.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a server certificate for a Cloud SQL instance.

     list
        (ALPHA) List all server certificates for a Cloud SQL instance.

     rollback
        (ALPHA) Roll back to the previous server certificate for a Cloud SQL
        instance.

     rotate
        (ALPHA) Rotate in the upcoming server certificate for a Cloud SQL
        instance.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud beta sql ssl server-certs

