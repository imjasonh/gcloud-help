NAME
    gcloud alpha dataplex encryption-config - manage Dataplex encryption
        configs

SYNOPSIS
    gcloud alpha dataplex encryption-config COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Dataplex encryption configs.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a Dataplex encryption config resource.

     describe
        (ALPHA) Describe an EncryptionConfig.

     update
        (ALPHA) Update an Encryption Config.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud dataplex encryption-config

