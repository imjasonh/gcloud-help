NAME
    gcloud alpha metastore services export - export metadata from a Dataproc
        Metastore service

SYNOPSIS
    gcloud alpha metastore services export COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Export metadata from a Dataproc Metastore service.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     gcs
        (ALPHA) Export metadata from a Dataproc Metastore service to Google
        Cloud Storage.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud metastore services export

        $ gcloud beta metastore services export

