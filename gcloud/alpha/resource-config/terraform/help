NAME
    gcloud alpha resource-config terraform - the command group for Terraform
        provider configuration

SYNOPSIS
    gcloud alpha resource-config terraform COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) The command group for Terraform provider configuration.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     generate-import
        (ALPHA) Generate Terraform import script for exported resources.

     init-provider
        (ALPHA) Generate main.tf file to configure Google Cloud Terraform
        Provider.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud beta resource-config terraform

