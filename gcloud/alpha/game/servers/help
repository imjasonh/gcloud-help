NAME
    gcloud alpha game servers - managed Cloud Game Servers

SYNOPSIS
    gcloud alpha game servers GROUP [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Managed Cloud Game Servers.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

GROUPS
    GROUP is one of the following:

     clusters
        (ALPHA) Deploy and manage Cloud Game Server Clusters.

     configs
        (ALPHA) Deploy and manage Cloud Game Server Configs.

     deployments
        (ALPHA) Deploy and manage Cloud Game Server Deployments.

     realms
        (ALPHA) Deploy and manage Cloud Game Server Realms.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud game servers

        $ gcloud beta game servers

