NAME
    gcloud alpha bms snapshot-schedule-policies - manage
        snapshot-schedule-policies in Bare Metal Solution

SYNOPSIS
    gcloud alpha bms snapshot-schedule-policies COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage snapshot-schedule-policies in Bare Metal Solution.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a Bare Metal Solution snapshot schedule policy.

     delete
        (ALPHA) Describe a Bare Metal solution snapshot schedule policy.

     describe
        (ALPHA) Describe a Bare Metal solution snapshot schedule policy.

     list
        (ALPHA) List Bare Metal Solution snapshot schedule policies in a
        project.

     update
        (ALPHA) Create a Bare Metal Solution snapshot schedule policy.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

