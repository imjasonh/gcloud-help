NAME
    gcloud alpha scc muteconfigs - manage Cloud SCC (Security Command Center)
        mute configs

SYNOPSIS
    gcloud alpha scc muteconfigs COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Cloud SCC (Security Command Center) mute configs.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a Cloud Security Command Center mute config.

     delete
        (ALPHA) Delete a Cloud Security Command Center mute config.

     get
        (ALPHA) Get a Cloud Security Command Center mute config.

     list
        (ALPHA) List Cloud Security Command Center mute configs.

     update
        (ALPHA) Update a Cloud Security Command Center mute config.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud scc muteconfigs

