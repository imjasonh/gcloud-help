NAME
    gcloud alpha gemini-cloud-assist investigations - create and manage Gemini
        Cloud Assist investigations

SYNOPSIS
    gcloud alpha gemini-cloud-assist investigations COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Create and manage Gemini Cloud Assist investigations.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     add-iam-policy-binding
        (ALPHA) Adds an IAM policy binding for an investigation.

     get-iam-policy
        (ALPHA) Get IAM policy for an investigation.

     remove-iam-policy-binding
        (ALPHA) Removes an IAM policy binding from an investigation.

     set-iam-policy
        (ALPHA) Sets IAM policy for an investigation.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

