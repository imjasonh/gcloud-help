NAME
    gcloud alpha firebase test ios locales - explore iOS locales available for
        testing

SYNOPSIS
    gcloud alpha firebase test ios locales COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Explore iOS locales available for testing.

EXAMPLES
    To list all available iOS locales which can be used for testing
    international applications, run:

        $ gcloud alpha firebase test ios locales list

    To view information about a specific locale, run:

        $ gcloud alpha firebase test ios locales describe LOCALE

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (ALPHA) Describe an iOS locale.

     list
        (ALPHA) List all iOS locales available for testing internationalized
        apps.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud firebase test ios locales

        $ gcloud beta firebase test ios locales

