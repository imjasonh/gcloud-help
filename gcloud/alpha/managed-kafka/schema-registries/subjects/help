NAME
    gcloud alpha managed-kafka schema-registries subjects - administer Managed
        Service for Apache Kafka schema-registries

SYNOPSIS
    gcloud alpha managed-kafka schema-registries subjects COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Administer Managed Service for Apache Kafka schema-registries.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (ALPHA) Describe a subject in a schema registry with all of its fields.

     update
        (ALPHA) Update the mode and compatibility of a subject.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

