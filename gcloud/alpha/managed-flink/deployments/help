NAME
    gcloud alpha managed-flink deployments - manage Managed Flink deployments

SYNOPSIS
    gcloud alpha managed-flink deployments COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Managed Flink deployments.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a BigQuery Engine for Apache Flink deployment.

     delete
        (ALPHA) Delete a BigQuery Engine for Apache Flink deployment.

     describe
        (ALPHA) Describe a BigQuery Engine for Apache Flink deployment.

     list
        (ALPHA) List all BigQuery Engine for Apache Flink deployments in a
        given location.

     update
        (ALPHA) Update a BigQuery Engine for Apache Flink deployment.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

