NAME
    gcloud alpha managed-flink deployments - manage Managed Flink deployments

SYNOPSIS
    gcloud alpha managed-flink deployments COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Managed Flink deployments.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a Managed Flink deployment.

     delete
        (ALPHA) Delete a Managed Flink deployment.

     describe
        (ALPHA) Describe a Managed Flink deployment.

     list
        (ALPHA) List all Managed Flink deployments in a given location.

     update
        (ALPHA) Update a Managed Flink deployment.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

