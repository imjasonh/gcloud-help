NAME
    gcloud alpha recommender recommender-configs describe - describe a
        Recommender Config

SYNOPSIS
    gcloud alpha recommender recommender-configs describe --location=LOCATION
        --project=PROJECT --recommender=RECOMMENDER [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Describe a Recommender Config based on given project, location, and
    recommender.

EXAMPLES
    To describe a Recommender Config:

        $ gcloud alpha recommender recommender-configs describe \
            --project=${PROJECT} --location=${LOCATION} \
            --recommender=${RECOMMENDER}

REQUIRED FLAGS
     --location=LOCATION
        Location

     --project=PROJECT
        Project number

     --recommender=RECOMMENDER
        Recommender for the recommender config

GCLOUD WIDE FLAGS
    These flags are available to all commands: --access-token-file, --account,
    --billing-project, --configuration, --flags-file, --flatten, --format,
    --help, --impersonate-service-account, --log-http, --project, --quiet,
    --trace-token, --user-output-enabled, --verbosity.

    Run $ gcloud help for details.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

