NAME
    gcloud alpha tasks locations - get information about Cloud Tasks locations

SYNOPSIS
    gcloud alpha tasks locations COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Get information about Cloud Tasks locations.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (ALPHA) Show details about a location.

     list
        (ALPHA) Lists the locations where Cloud Tasks is available.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud tasks locations

        $ gcloud beta tasks locations

