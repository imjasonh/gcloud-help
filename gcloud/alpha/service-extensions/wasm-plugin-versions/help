NAME
    gcloud alpha service-extensions wasm-plugin-versions - interact with and
        manage Service Extensions WasmPluginVersions

SYNOPSIS
    gcloud alpha service-extensions wasm-plugin-versions COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Interact with and manage Service Extensions WasmPluginVersions.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a WasmPluginVersion.

     delete
        (ALPHA) Delete a WasmPluginVersion.

     describe
        (ALPHA) Show details about a WasmPluginVersion.

     list
        (ALPHA) List all WasmPluginVersions for a WasmPlugin.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

