NAME
    gcloud alpha compute resource-policies create vm-maintenance - create
        Compute Engine VM Maintenance Resource Policies

SYNOPSIS
    gcloud alpha compute resource-policies create vm-maintenance COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Create Compute Engine VM Maintenance Resource Policies.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     concurrency-limit
        (ALPHA) Create a Compute Engine VM Maintenance Resource Policy.

     maintenance-window
        (ALPHA) Create a Compute Engine VM Maintenance Resource Policy.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

