NAME
    gcloud alpha compute packet-mirrorings - manage Compute Engine packet
        mirroring resources

SYNOPSIS
    gcloud alpha compute packet-mirrorings COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Compute Engine packet mirroring resources.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create a Compute Engine packet mirroring policy.

     delete
        (ALPHA) Delete a Compute Engine packet mirroring policy.

     describe
        (ALPHA) Describe a Compute Engine packet mirroring policy.

     list
        (ALPHA) List Google Compute Engine packet mirroring policies.

     update
        (ALPHA) Update a Compute Engine packet mirroring policy.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud compute packet-mirrorings

        $ gcloud beta compute packet-mirrorings

