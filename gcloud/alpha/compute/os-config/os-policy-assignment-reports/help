NAME
    gcloud alpha compute os-config os-policy-assignment-reports - manage OS
        policy asssignment reports

SYNOPSIS
    gcloud alpha compute os-config os-policy-assignment-reports COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage OS policy asssignment reports.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (ALPHA) Describe an OS policy assignment report.

     list
        (ALPHA) List OS policy assignment reports.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud compute os-config os-policy-assignment-reports

