NAME
    gcloud alpha compute tpus tpu-vm accelerator-types - list or Describe
        Available Cloud TPU VM node accelerator types

SYNOPSIS
    gcloud alpha compute tpus tpu-vm accelerator-types COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) List or Describe Available Cloud TPU VM node accelerator types.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (ALPHA) Describe an accelerator type available for Cloud TPU VM nodes.

     list
        (ALPHA) List available accelerator types for Cloud TPU VM nodes.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud compute tpus tpu-vm accelerator-types

