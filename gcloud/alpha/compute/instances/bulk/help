NAME
    gcloud alpha compute instances bulk - manipulate multiple Compute Engine
        virtual machines with single command executions

SYNOPSIS
    gcloud alpha compute instances bulk COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manipulate multiple Compute Engine virtual machines with single
    command executions.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Create multiple Compute Engine virtual machines.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud compute instances bulk

        $ gcloud beta compute instances bulk

