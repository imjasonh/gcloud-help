NAME
    gcloud alpha data-catalog taxonomies policy-tags - manage policy tags in
        Data Catalog

SYNOPSIS
    gcloud alpha data-catalog taxonomies policy-tags COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage policy tags in Data Catalog.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     add-iam-policy-binding
        (ALPHA) Add an IAM policy binding to a Data Catalog policy tag.

     describe
        (ALPHA) Describe a Policy Tag Manager Policy tag.

     get-iam-policy
        (ALPHA) Get the IAM policy for a Data Catalog Policy Tag.

     list
        (ALPHA) List Cloud Policy Tag Manager policy tags.

     remove-iam-policy-binding
        (ALPHA) Remove an IAM policy binding from a Data Catalog policy tag.

     set-iam-policy
        (ALPHA) Set the IAM policy for a Data Catalog Policy Tag.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud data-catalog taxonomies policy-tags

        $ gcloud beta data-catalog taxonomies policy-tags

