NAME
    gcloud alpha anthos config operations - get and list operations for Anthos
        Config Controller instances

SYNOPSIS
    gcloud alpha anthos config operations COMMAND [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Get and list operations for Anthos Config Controller instances.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (ALPHA) Describe Anthos Config Controller operations.

     list
        (ALPHA) List Anthos Config Controller operations.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud anthos config operations

