NAME
    gcloud alpha container hub policycontroller content templates - manage
        Policy Controller content templates

SYNOPSIS
    gcloud alpha container hub policycontroller content templates COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Policy Controller content templates.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     disable
        (ALPHA) Disable template installation for Policy Controller content.

     enable
        (ALPHA) Installs the template library for Policy Controller.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. These variants are also available:

        $ gcloud container hub policycontroller content templates

        $ gcloud beta container hub policycontroller content templates

