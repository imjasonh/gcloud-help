NAME
    gcloud alpha container ai recommender manifests - generate optimized
        Kubernetes manifests

SYNOPSIS
    gcloud alpha container ai recommender manifests COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) (DEPRECATED) Generate optimized Kubernetes manifests.

    This command group is deprecated. Use gcloud alpha container ai profiles
    command group instead.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) (DEPRECATED) Generate ready-to-deploy Kubernetes manifests with
        compute, load balancing, and autoscaling capabilities.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

