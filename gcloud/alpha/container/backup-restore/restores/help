NAME
    gcloud alpha container backup-restore restores - backup for GKE Restores

SYNOPSIS
    gcloud alpha container backup-restore restores COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Backup for GKE Restores.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Creates a restore.

     delete
        (ALPHA) Delete a restore.

     describe
        (ALPHA) Describe a restore.

     get-iam-policy
        (ALPHA) Get the IAM policy for a restore.

     list
        (ALPHA) List restores.

     set-iam-policy
        (ALPHA) Set the IAM policy for a restore.

     update
        (ALPHA) Update a restore.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud beta container backup-restore restores

