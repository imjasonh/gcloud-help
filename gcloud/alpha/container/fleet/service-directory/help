NAME
    gcloud alpha container fleet service-directory - manage the Service
        Directory Feature

SYNOPSIS
    gcloud alpha container fleet service-directory COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage the Service Directory Feature.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     describe
        (ALPHA) Describe the status of the Service Directory Feature resource.

     disable
        (ALPHA) Disable the Service Directory Feature.

     enable
        (ALPHA) Enable the Service Directory Feature.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

