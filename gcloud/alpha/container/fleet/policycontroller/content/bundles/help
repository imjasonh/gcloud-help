NAME
    gcloud alpha container fleet policycontroller content bundles - manage
        Policy Controller content bundles

SYNOPSIS
    gcloud alpha container fleet policycontroller content bundles COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Manage Policy Controller content bundles.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     remove
        (ALPHA) Removes a bundle installation for Policy Controller content.

     set
        (ALPHA) Sets bundle installation for Policy Controller content.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist. This variant is also available:

        $ gcloud beta container fleet policycontroller content bundles

