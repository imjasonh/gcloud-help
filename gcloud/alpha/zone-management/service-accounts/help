NAME
    gcloud alpha zone-management service-accounts - zone Management Service
        Account gcloud commands

SYNOPSIS
    gcloud alpha zone-management service-accounts GROUP | COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Zone Management Service Account gcloud commands.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

GROUPS
    GROUP is one of the following:

     keys
        (ALPHA) Zone Management Service Account Keys gcloud commands.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) create service account.

     delete
        (ALPHA) delete service account.

     describe
        (ALPHA) describe service account.

     list
        (ALPHA) List service accounts.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

