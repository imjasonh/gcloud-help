NAME
    gcloud alpha zone-management identity-providers - zone Management Service
        Identity Provider gcloud commands

SYNOPSIS
    gcloud alpha zone-management identity-providers COMMAND
        [GCLOUD_WIDE_FLAG ...]

DESCRIPTION
    (ALPHA) Zone Management Service Identity Provider gcloud commands.

GCLOUD WIDE FLAGS
    These flags are available to all commands: --help.

    Run $ gcloud help for details.

COMMANDS
    COMMAND is one of the following:

     create
        (ALPHA) Creates an identity provider for a Google Destributed Cloud
        zone.

     delete
        (ALPHA) Delete an identity provider on a Google Distributed Cloud zone.

     describe
        (ALPHA) Describes an identity provider on a Google Distributed Cloud
        zone.

     list
        (ALPHA) List identity providers created on Google Distributed Cloud
        zone.

NOTES
    This command is currently in alpha and might change without notice. If this
    command fails with API permission errors despite specifying the correct
    project, you might be trying to access an API with an invitation-only early
    access allowlist.

